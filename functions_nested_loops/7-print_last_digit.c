#include "main.h"

/**
 * print_last_digit - imprime el utimo digito
 * @n: es el ulimo digito
 * Return: 0
 */
int print_last_digit(int r)
{
	if (r % 10)
	{
		return (r);
	}
	else
	{
		return (0);
	}

}
