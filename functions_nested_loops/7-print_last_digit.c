#include "main.h"

/**
 * print_last_digit - imprime el utimo digito
 * @n: es el ulimo digito
 * Return: 0
 */
int print_last_digit(int n)
{
	if (n % 10)
	{
		return (n);
	}
	else
	{
		return (0);
	}

}
