#include "lists.h"

/**
 * _strlen - Cuenta los caracteres de una cadena
 * @s: Cadena
 * Return: 0
 */
int _strlen(const char *s)
{
	int len;

	for (len = 0; s[len] != '\0'; len++)
	{
	}
	return (len);
}
